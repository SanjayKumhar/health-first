<!DOCTYPE html>
<html lang="en">
  <head>
    <title>Doctors List</title>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Concert+One&family=Ephesis&family=Indie+Flower&family=Oswald:wght@200&family=Stick+No+Bills:wght@200&display=swap"
      rel="stylesheet"
    />
    <style type="text/css">
      .project-title-header {
        position: fixed;
        top: 0px;
        left: 0px;
        width: 100%;
        padding-top: 10px;
        padding-bottom: 10px;
        height: 60px;
        background-color: rgb(0, 140, 255);
      }

      .project-name-a {
        text-decoration: none;
        font-size: 25px;
        padding-left: 25px;
        font-family: "Oswald", sans-serif;
        color: white;
      }

      .doctor-details {
        margin-top: 100px;
      }

      table,
      td,
      th {
        border: 1px solid rgb(3, 3, 3);
        text-align: center;
      }

      table {
        border-collapse: collapse;
        table-layout: fixed;
        width: 60%;
        font-family: cursive;
        font-weight: normal;
        font-size: 15px;
      }

      .table-data {
        display: flex;
        align-items: center;
        justify-content: center;
      }

      th,
      td {
        padding: 15px;
      }

      p {
        text-align: center;
        font-family: cursive;
        font-weight: normal;
      }
    </style>
  </head>

  <body>
    <div class="project-title-header">
      <h2>
        <a href="index.html" class="project-name-a">Health First . com</a>
      </h2>
    </div>
    <div class="doctor-details">
      <div>
        <h2 style="text-align: center; font-size: 25px; font-family: cursive;">
          List of available doctors
        </h2>
      </div>
      <div class="table-data">
        <table style="background-color: lightblue;">
          <tr style="font-size: 18px;">
            <th>Registration No.</th>
            <th>Name</th>
            <th>Speciality</th>
          </tr>

          <% if(doctorData.length!=0){ var i=1;
          doctorData.forEach(function(data){ %>
          <tr>
            <td>
              <%=data.registeration %>
            </td>
            <td>
              <%=data.name %>
            </td>
            <td>
              <%=data.speciality %>
            </td>
          </tr>
          <% i++; }) %> <% } else{ %>
          <tr>
            <td colspan="3">No Data Found</td>
          </tr>
          <% } %>
        </table>
      </div>
      <div>
        <br>
        <p><b>*NOTE:</b> Copy the registration number of the required doctor</p>
      </div>
    </div>
  </body>
</html>
